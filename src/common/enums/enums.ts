export enum PagePath {
  GARAGE = '/garage',
  WINNERS = '/winners',
  NOT_FOUND = '/not-found',
}

export enum DefaultPath {
  EMPTY = '',
  SLASH = '/',
}

export enum SortField {
  ID = 'id',
  WINS = 'wins',
  TIME = 'time',
}

export enum SortOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}
